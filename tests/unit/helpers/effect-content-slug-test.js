import { effectContentSlug } from '../../../helpers/effect-content-slug';
import { module, test } from 'qunit';

module('Unit | Helper | effect content slug');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = effectContentSlug(42);
  assert.ok(result);
});
