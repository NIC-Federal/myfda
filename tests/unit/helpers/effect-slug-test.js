import { effectSlug } from '../../../helpers/effect-slug';
import { module, test } from 'qunit';

module('Unit | Helper | effect slug');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = effectSlug(42);
  assert.ok(result);
});
