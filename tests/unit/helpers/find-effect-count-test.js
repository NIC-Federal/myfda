import { findEffectCount } from '../../../helpers/find-effect-count';
import { module, test } from 'qunit';

module('Unit | Helper | find effect count');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = findEffectCount(42);
  assert.ok(result);
});
