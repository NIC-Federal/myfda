import { progressBarPercentage } from '../../../helpers/progress-bar-percentage';
import { module, test } from 'qunit';

module('Unit | Helper | progress bar percentage');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = progressBarPercentage(42);
  assert.ok(result);
});
