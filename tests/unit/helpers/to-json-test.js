import { toJson } from '../../../helpers/to-json';
import { module, test } from 'qunit';

module('Unit | Helper | to json');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = toJson(42);
  assert.ok(result);
});
